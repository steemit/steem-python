machine:
    services:
        - docker

dependencies:
  override:
    - pip install tox tox-pyenv
    - pyenv local 2.7.10 3.3.3 3.4.3 3.5.0 3.6.4

test:
    override:
        - docker build -t steemit/steem-python .
